/** @type {import('tailwindcss').Config} */
export default {
  darkMode: 'class', //obligatoire pour utiliser le mode sombre via la classe
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      keyframes: {
        fadeIn: {
          '0%': { opacity: '0' },
          '100%': { opacity: '1' },
        },
      },
    },
  },
  plugins: [],
}
